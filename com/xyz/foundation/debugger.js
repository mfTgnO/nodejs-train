/*
* Node.js Debugger
* https://www.javatpoint.com/nodejs-debugger
*
* Node.js provides a simple TCP based protocol and built-in debugging client. For debugging your JavaScript file,
* you can use debug argument followed by the js file name you want to debug.
* */
for (let i = 0; i < 10; i++) {
    if (i % 2 === 0) {
        console.log(i);
    }
}